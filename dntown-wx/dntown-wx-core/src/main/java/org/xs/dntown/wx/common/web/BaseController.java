package org.xs.dntown.wx.common.web;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * 控制器支持类
 */
public abstract class BaseController {
	
	public final Logger log = LoggerFactory.getLogger(getClass().getSuperclass().getName());
}
